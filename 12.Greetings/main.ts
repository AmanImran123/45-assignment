// exercise 12
let members: string[] = ['ali', 'atif', 'shadan', 'sara'];
let message: string='kal kon se class hai';
for (let index = 0; index < members.length; index++) {
  console.log(message + members[index]);
}
